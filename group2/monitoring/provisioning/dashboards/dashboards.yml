apiVersion: 1

providers:
  - name: Multi-DB
    orgId: 1
    folder: Multi-DB
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10

    options:
      path: /var/lib/grafana/dashboards